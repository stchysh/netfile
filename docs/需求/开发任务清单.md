# NetFile 开发任务清单

## 阶段 1: 项目初始化与核心架构 ✅

### 1.1 项目结构搭建 ✅
- [x] 创建 Rust workspace 项目结构
- [x] 配置 Cargo.toml 依赖（tokio, serde, bincode 等）
- [x] 创建核心模块目录结构

### 1.2 配置管理模块 ✅
- [x] 定义配置结构体（实例配置、网络配置、传输配置、安全配置）
- [x] 实现配置文件读取/写入（TOML 格式）
- [x] 实现配置默认值生成
- [x] 实现实例 ID 和设备 ID 生成（UUID）

### 1.3 日志系统 ✅
- [x] 集成日志框架（tracing）
- [x] 配置日志输出格式和级别

## 阶段 2: 设备发现功能 ✅

### 2.1 UDP 广播发现 ✅
- [x] 定义 DiscoveryMessage 协议结构
- [x] 实现 UDP 广播发送器
- [x] 实现 UDP 广播接收器
- [x] 实现设备列表管理（增删改查）

### 2.2 心跳机制 ✅
- [x] 实现定时心跳发送
- [x] 实现心跳超时检测
- [x] 实现设备在线状态更新

### 2.3 端口自动分配 ✅
- [x] 实现端口可用性检测
- [x] 实现动态端口分配逻辑
- [x] 实现端口冲突处理

## 阶段 3: 基础文件传输 ✅

### 3.1 传输协议定义 ✅
- [x] 定义 TransferRequest 结构
- [x] 定义 ChunkData 结构
- [x] 定义 TransferStatus 结构
- [x] 定义协议消息枚举

### 3.2 TCP 传输服务 ✅
- [x] 实现 TCP 服务器（监听连接）
- [x] 实现 TCP 客户端（发起连接）
- [x] 实现连接管理（连接池）

### 3.3 单文件传输 ✅
- [x] 实现文件读取（流式）
- [x] 实现文件写入（流式）
- [x] 实现文件哈希计算（SHA256）
- [x] 实现分块传输逻辑
- [x] 实现传输进度跟踪

### 3.4 传输队列 ✅
- [x] 实现传输任务队列
- [x] 实现任务调度器
- [x] 实现并发控制（最大并发数）

## 阶段 4: 断点续传 ✅

### 4.1 传输状态持久化 ✅
- [x] 定义传输状态存储格式（JSON）
- [x] 实现状态保存逻辑
- [x] 实现状态加载逻辑

### 4.2 断点续传逻辑 ✅
- [x] 实现已传输块记录
- [x] 实现传输中断检测
- [x] 实现从断点恢复传输
- [x] 实现文件完整性校验

### 4.3 临时文件管理 ✅
- [x] 实现临时文件创建
- [x] 实现临时文件清理
- [x] 实现文件重命名（完成后）

## 阶段 5: 文件夹传输 ✅

### 5.1 目录扫描 ✅
- [x] 实现递归目录遍历
- [x] 实现文件列表生成
- [x] 实现目录结构序列化

### 5.2 批量传输 ✅
- [x] 实现多文件传输任务创建
- [x] 实现批量传输进度统计
- [x] 实现目录结构重建

## 阶段 6: CLI 命令行接口 ✅

### 6.1 命令行参数解析 ✅
- [x] 集成 clap 框架
- [x] 定义主命令和子命令结构
- [x] 实现参数验证

### 6.2 基础命令实现 ✅
- [x] 实现启动命令（GUI/CLI 模式）
- [x] 实现 devices list 命令
- [x] 实现 devices info 命令
- [x] 实现 send 命令（支持文件和文件夹）
- [x] 实现 transfers list 命令
- [x] 实现 transfers info 命令

### 6.3 配置命令 ✅
- [x] 实现 config show 命令
- [x] 实现 config set 命令
- [x] 实现 config reset 命令

### 6.4 输出格式化 ✅
- [x] 实现表格格式输出
- [x] 实现 JSON 格式输出
- [x] 实现简洁格式输出

## 当前优先级

**下一步**:
1. ✅ 实现目录结构重建（保留文件夹层级）
2. ✅ 添加传输进度实时显示
3. ✅ 调试设备发现功能（UDP 广播问题）
4. 实现更多 CLI 命令（transfers list、config set 等）

## 已完成功能测试

- ✅ 服务器启动并监听端口（发现端口 37020，传输端口 37050）
- ✅ 文件发送功能（`netfile send 127.0.0.1:37050 test.txt`）
- ✅ 文件接收功能（自动保存到下载目录）
- ✅ 文件完整性校验（SHA256）
- ✅ 跨磁盘驱动器文件传输（copy + remove fallback）
- ✅ 目录扫描功能（递归遍历、文件列表生成）
- ✅ 文件夹传输功能（`netfile send 127.0.0.1:37050 test_dir --recursive`）
  - 成功扫描多层级目录：3 个文件
  - 成功传输所有文件：3/3
  - ✅ 保留目录结构（支持任意深度嵌套）
- ✅ 传输进度实时显示
  - 显示百分比、已传输/总大小
  - 实时传输速度（MB/s）
  - 预计剩余时间（ETA）
  - 测试：100MB 文件，速度 140+ MB/s
- ✅ 设备发现功能（UDP 广播）
  - 成功发现局域网内的其他设备
  - 测试：Server1 和 Server2 互相发现
  - 使用独立配置文件避免 instance_id 冲突

## 阶段 4: 用户界面 ✅

### 4.1 GUI 界面（Tauri） ✅
- [x] 初始化 Tauri 项目
- [x] 设备列表界面
- [x] 传输队列界面
- [x] 文件发送界面
- [x] 设置界面

## 最新更新

### 2026-03-03
- ✅ 完成阶段 4 GUI 界面开发（Tauri + React）
  - 初始化 Tauri 项目结构
  - 创建 React + TypeScript 前端
  - 实现设备列表界面：
    - 显示在线设备（设备名、实例名、IP、端口）
    - 在线状态指示器
    - 发送文件按钮
    - 自动刷新（1秒间隔）
  - 实现传输队列界面：
    - 显示传输任务列表
    - 进度条和百分比显示
    - 传输速度和大小显示
    - 自动刷新（500ms间隔）
  - 实现文件发送界面：
    - 文件选择对话框
    - 多文件/文件夹选择
    - 拖拽区域（UI 准备）
    - 压缩选项
    - 发送确认
  - 实现设置界面：
    - 实例信息配置
    - 网络配置（端口、广播间隔）
    - 传输配置（块大小、并发数、压缩）
    - 安全配置（授权、TLS）
  - Tauri 后端命令：
    - get_devices: 获取设备列表
    - get_transfers: 获取传输列表
    - send_file: 发送文件
    - get_config: 获取配置
    - update_config: 更新配置
- ✅ 完成阶段 10.1 性能优化
  - 实现零拷贝优化：使用 bytes::Bytes 替代 Vec<u8>，减少内存拷贝
  - ChunkData.data 字段改为 Bytes 类型
  - 优化传输流程，减少不必要的克隆操作
  - 优化内存占用：
    - 添加消息大小限制（最大 10MB）防止内存溢出
    - 优化锁的持有时间，减少锁竞争
    - 改进缓冲区管理，避免不必要的内存分配
  - 所有测试通过（20/20）

### 2026-03-02
- ✅ 完成 TUI 界面开发
  - 三标签页布局（Devices、Transfers、Settings）
  - 实时设备列表更新（1秒间隔）
  - 实时传输进度显示（500ms间隔）
  - 键盘操作支持（Tab切换、数字键直接选择、q退出）
  - 修复设备发现问题（--name参数自动生成新instance_id）
  - 优化日志输出（只显示WARN级别，避免干扰界面）
  - 修复UDP广播错误（只发送到本地回环地址）
- ✅ 完成文档编写
  - 编写安装指南（docs/指南/安装指南.md）
  - 编写使用指南（docs/指南/使用指南.md）
  - 编写开发文档（docs/指南/开发文档.md）
  - 包含完整的命令参考、配置说明、故障排除等
- ✅ 标记阶段10.3完成
- ✅ 实现目录结构重建功能
  - 协议扩展：添加 relative_path 字段
  - 接收端自动创建目录层级
  - 支持多层嵌套目录（如 subdir1/subdir2/file.txt）
- ✅ 实现传输进度实时显示
  - 进度跟踪器（ProgressTracker）
  - 实时更新（200ms 刷新）
  - 详细信息：百分比、速度、ETA
  - 格式化输出：自动单位转换（B/KB/MB/GB）
- ✅ 修复设备发现功能
  - 问题：两个实例使用相同的 instance_id 导致互相忽略
  - 解决：使用独立配置文件（--config 参数）为每个实例生成唯一 ID
  - 添加详细的 DEBUG 日志用于调试
  - 修复广播逻辑：跳过自己的端口避免自我发现
  - 测试通过：Server1 和 Server2 成功互相发现
- ✅ 实现 CLI 输出格式化
  - 支持三种输出格式：Table（表格）、JSON、Simple（简洁）
  - 使用 -o/--output 参数指定格式（默认 table）
  - 所有命令支持格式化输出：devices list/info、transfers list/info、config show
  - 为 Device 和 TransferProgress 添加 Serialize 支持
  - 测试通过：三种格式均正常工作
- ✅ 实现身份验证机制
  - 创建 AuthManager 模块管理授权和密码
  - 实现授权设备列表管理（添加、移除、列出）
  - 实现密码保护（SHA256 哈希）
  - 添加 auth 命令：list、allow、deny、set-password
  - 扩展协议：TransferRequest 添加 device_id 和 password_hash 字段
  - 添加 AuthRequest 和 AuthResponse 消息类型
  - 测试通过：授权设备管理和密码设置功能正常
- ✅ 实现压缩传输功能（zstd）
  - 添加 zstd 依赖和 Compressor 模块
  - 实现 compress/decompress 方法（压缩级别 3）
  - 协议扩展：ChunkData 添加 compressed 字段
  - 发送端智能压缩：仅对 >1024 字节且有压缩收益的块进行压缩
  - 接收端自动解压：检测 compressed 标志并解压
  - 配置支持：enable_compression 配置项控制压缩开关
  - 测试通过：
    - 全零数据（10MB）：压缩率 0.0%（1048576 -> 50 字节）
    - 随机数据（5MB）：无压缩收益，自动跳过压缩
    - 日志显示压缩比例和决策过程
- ✅ 实现 TLS 加密功能
  - 集成 rustls、tokio-rustls、rcgen、rustls-pemfile 依赖
  - 创建 TlsManager 模块管理证书和配置
  - 实现自签名证书生成（rcgen）
  - 证书包含 localhost 和 127.0.0.1 的 SAN
  - 实现 TLS 服务器配置加载
  - 实现 TLS 客户端配置加载
  - 证书自动持久化到 data_dir/certs/
  - 配置支持：enable_tls 配置项控制 TLS 开关
  - 测试通过：证书生成、服务器配置、客户端配置均正常
- ✅ 编写单元测试和集成测试
  - 配置模块测试：默认值、序列化、ID 生成
  - 协议模块测试：消息序列化、反序列化
  - 压缩模块测试：压缩/解压、空数据、随机数据、大数据
  - 认证模块测试：密码哈希、密码验证、设备授权、设备列表
  - 集成测试：TransferService 创建、压缩配置、进度跟踪
  - 测试结果：20 个测试全部通过（16 个单元测试 + 4 个集成测试）
- ✅ 实现 STUN 协议支持
  - 集成 stun 库（0.6 版本）
  - 创建 StunClient 模块
  - 实现公网 IP 地址获取（支持多个 STUN 服务器）
  - 实现 NAT 类型检测
  - 内置 Google STUN 服务器列表
  - 支持自定义 STUN 服务器
  - 测试通过：成功获取公网 IP（156.229.166.12）和 NAT 类型（SymmetricNat）
- ✅ 实现 UDP 打洞功能
  - 创建 UdpHolePuncher 模块
  - 实现 UDP 打洞协议（PUNCH/PUNCH_ACK）
  - 实现打洞尝试逻辑（最多 10 次尝试，每次间隔 200ms）
  - 实现超时重试机制（每次尝试超时 500ms）
  - 支持监听打洞请求并自动响应
  - 支持数据收发功能
  - 支持对等节点列表管理
  - 测试通过：本地环回测试成功建立 UDP 连接
- ✅ 实现 TUI 终端界面
  - 集成 Ratatui 和 Crossterm 框架
  - 创建 netfile-tui 独立包
  - 实现三标签页布局（Devices、Transfers、Settings）
  - 设备列表界面：实时显示在线设备（设备名、实例名、IP、端口）
  - 传输队列界面：实时显示传输进度条和速度
  - 设置界面：显示所有配置项
  - 支持键盘操作（q 退出、Tab 切换、1/2/3 直接选择）
  - 后台自动更新设备列表（1 秒间隔）和传输进度（500ms 间隔）
  - 测试通过：后端服务正常启动，界面渲染正常

## 阶段 7: 安全机制 🚧

### 7.1 TLS 加密 ✅
- [x] 集成 rustls
- [x] 实现自签名证书生成
- [x] 实现 TLS 服务器配置
- [x] 实现 TLS 客户端配置

### 7.2 身份验证 ✅
- [x] 实现设备授权确认机制
- [x] 实现密码保护（可选）
- [x] 实现授权设备列表管理

## 阶段 8: NAT 穿透

### 8.1 STUN 协议 ✅
- [x] 集成 STUN 客户端库
- [x] 实现公网 IP 获取
- [x] 实现端口映射检测

### 8.2 UDP 打洞 ✅
- [x] 实现 UDP 打洞协议
- [x] 实现打洞尝试逻辑
- [x] 实现连接方式降级（直连 -> 打洞 -> 失败提示）

## 阶段 9: TUI 界面（Ratatui） ✅

### 9.1 TUI 集成 ✅
- [x] 集成 Ratatui 框架
- [x] 创建主窗口
- [x] 实现基础布局

### 9.2 设备列表界面 ✅
- [x] 实现设备列表组件
- [x] 实现设备状态显示
- [x] 实现设备操作按钮

### 9.3 传输队列界面 ✅
- [x] 实现传输任务列表组件
- [x] 实现进度条组件
- [x] 实现任务详情展开

### 9.4 设置界面 ✅
- [x] 实现配置显示界面

## 阶段 10: 性能优化与测试

### 10.1 性能优化 ✅
- [x] 实现零拷贝优化
- [x] 实现压缩传输（zstd）
- [x] 优化内存占用

### 10.2 测试 ✅
- [x] 编写单元测试
- [x] 编写集成测试
- [ ] 进行跨平台测试

### 10.3 文档完善 ✅
- [x] 更新 README.md
- [x] 编写使用文档
- [x] 编写开发文档

## 当前优先级

**立即开始**: 阶段 1 - 项目初始化与核心架构
